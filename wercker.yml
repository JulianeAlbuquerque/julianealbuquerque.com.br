box: nodesource/trusty

build:
  steps:
    - npm-install
    - script:
        name: build
        code: npm run build:all

deploy:
  steps:
    - gh-pages:
        token: $GH_TOKEN
        domain: julianealbuquerque.com.br
        basedir: /dist